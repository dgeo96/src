/* GNU ld script
   Use the shared library, but some functions are only in
   the static library, so try that secondarily.  */
OUTPUT_FORMAT(elf32-littlearm)
GROUP ( /usr/local/arm-linux/lib/soft-float/libpthread.so.0 /usr/local/arm-linux/lib/soft-float/libpthread_nonshared.a )
